{ inputs , pkgs, ... }:
{
  home.packages = with pkgs; [
    emacs
  ];
  xdg.configFile = {
    "early-init.el".source = "${inputs.self}/emacs/early-init.el";
    "init.el".source = "${inputs.self}/emacs/init.el";
    "config.org".source = "${inputs.self}/emacs/config.org";
  };
}
